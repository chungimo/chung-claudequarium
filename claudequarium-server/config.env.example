# Claudequarium Server Configuration
# Copy this file to .env and adjust as needed

# Server binding
# - 0.0.0.0 = accept connections from any IP (network accessible)
# - 127.0.0.1 = localhost only
HOST=0.0.0.0

# Server port
PORT=4000
